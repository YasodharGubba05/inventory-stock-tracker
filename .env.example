# Local / Docker Compose
DATABASE_URL=postgresql+asyncpg://inventory:inventory@localhost:5432/inventory

# Railway: set DATABASE_URL=${{Postgres.DATABASE_URL}} in the dashboard
# (postgresql:// URLs are auto-converted to postgresql+asyncpg://)

PORT=8000
LOG_LEVEL=INFO
LOG_JSON=true
DEBUG=false
